<%# sp %>
<div class="contents public-users-registrations-confirm sp">
  <h2>入力情報の確認</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table>
      <tbody>
        <tr>
          <td colspan="2"><h3>お名前</h3><p>予約時の情報として使用されます。</p></td>
        </tr>
        <tr>
          <td><%= f.label :name_family %>：<%= f.text_field :name_family, value: @user.name_family, readonly: true %></td>
          <td><%= f.label :name_first %>：<%= f.text_field :name_first, value: @user.name_first, readonly: true %></td>
        </tr>
        <tr>
          <td><%= f.label :name_family_kana %>：<%= f.text_field :name_family_kana, value: @user.name_family_kana, readonly: true %></td>
          <td><%= f.label :name_first_kana %>：<%= f.text_field :name_first_kana, value: @user.name_first_kana, readonly: true %></td>
        </tr>
        <tr>
          <td colspan="2"><h3><%= f.label :phone_number %></h3></td>
        </tr>
        <tr>
          <td><%= f.text_field :phone_number, value: @user.phone_number, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td colspan="2"><h3><%= f.label :email %></h3><p>ログイン時のIDとして使用されます。また、予約の確認や、メールマガジンの配信も行われます。</p></td>
        </tr>
        <tr>
          <td><%= f.email_field :email, value: @user.email, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td colspan="2">
            <h3><%= f.label :password %></h3>
          </td>
        </tr>
        <tr>
          <td><%= blind(@user.password) %></td>
          <td><%= f.hidden_field :password, value: @user.password %></td>
          <td></td>
        </tr>
      </tbody>
    </table>

    <div class="actions">
      <%= f.submit "修正する", class: 'btn btn-outline-dark', name: :back %>
      <%= f.submit "確認メールを送信", class: 'btn btn-success' %>
    </div>
  <% end %>

  <%= render "public/users/shared/links" %>

</div>

<%# pc %>
<div class="contents public-users-registrations-confirm pc">
  <h2>入力情報の確認</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table>
      <tbody>
        <tr>
          <td colspan="3"></td>
        </tr>
        <tr>
          <td><h3>お名前</h3><p>予約時の情報として使用されます。</p></td>
          <td><%= f.label :name_family %>：<%= f.text_field :name_family, value: @user.name_family, readonly: true %></td>
          <td><%= f.label :name_first %>：<%= f.text_field :name_first, value: @user.name_first, readonly: true %></td>
        </tr>
        <tr>
          <td></td>
          <td><%= f.label :name_family_kana %>：<%= f.text_field :name_family_kana, value: @user.name_family_kana, readonly: true %></td>
          <td><%= f.label :name_first_kana %>：<%= f.text_field :name_first_kana, value: @user.name_first_kana, readonly: true %></td>
        </tr>
        <tr>
          <td><h3><%= f.label :phone_number %></h3></td>
          <td><%= f.text_field :phone_number, value: @user.phone_number, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td><h3><%= f.label :email %></h3><p>ログイン時のIDとして使用されます。また、予約の確認や、メールマガジンの配信も行われます。</p></td>
          <td colspan="2"><%= f.email_field :email, value: @user.email, readonly: true %></td>
        </tr>
        <tr>
          <td>
            <h3><%= f.label :password %></h3>
            <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %>文字以上)</em>
            <% end %>
          </td>
          <td><%= blind(@user.password) %></td>
          <td><%= f.hidden_field :password, value: @user.password %></td>
          <td></td>
        </tr>
      </tbody>
    </table>

    <div class="actions">
      <%= f.submit "修正する", class: 'btn btn-outline-dark', name: :back %>
      <%= f.submit "確認メールを送信", class: 'btn btn-success' %>
    </div>
  <% end %>

  <%= render "public/users/shared/links" %>

</div>
