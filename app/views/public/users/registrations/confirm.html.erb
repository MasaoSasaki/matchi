<%# sp %>
<div class="contents public-users-registrations-confirm sp">
  <h2>入力情報の確認</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table>
      <tbody>
        <tr>
          <td colspan="2"><h3>お名前</h3><p>予約時の情報として使用されます。</p></td>
        </tr>
        <tr>
          <td><%= f.label :name_family, '姓：' %><%= f.text_field :name_family, value: @user.name_family, readonly: true %></td>
          <td><%= f.label :name_first, '名：' %><%= f.text_field :name_first, value: @user.name_first, readonly: true %></td>
        </tr>
        <tr>
          <td><%= f.label :name_family_kana, 'セイ：' %><%= f.text_field :name_family_kana, value: @user.name_family_kana, readonly: true %></td>
          <td><%= f.label :name_first_kana, 'メイ：' %><%= f.text_field :name_first_kana, value: @user.name_first_kana, readonly: true %></td>
        </tr>
        <tr>
          <td colspan="2"><h3><%= f.label :phone_number, '電話番号' %></h3></td>
        </tr>
        <tr>
          <td><%= f.text_field :phone_number, value: @user.phone_number, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td colspan="2"><h3><%= f.label :email, 'メールアドレス' %></h3><p>ログイン時のIDとして使用されます。また、予約の確認や、メールマガジンの配信も行われます。</p></td>
        </tr>
        <tr>
          <td><%= f.email_field :email, value: @user.email, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td colspan="2">
            <h3><%= f.label :password, 'パスワード' %></h3>
          </td>
        </tr>
        <tr>
          <td><%= @password %></td>
          <td><%= f.hidden_field :password, value: @user.password %></td>
          <td></td>
        </tr>
      </tbody>
    </table>

    <div class="actions">
      <%= f.submit "修正する", class: 'btn btn-default', name: :back %>
      <%= f.submit "登録する", class: 'btn btn-primary' %>
    </div>
  <% end %>

  <%= render "public/users/shared/links" %>

</div>

<%# pc %>
<div class="contents public-users-registrations-confirm pc">
  <h2>入力情報の確認</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table>
      <tbody>
        <tr>
          <td colspan="3"></td>
        </tr>
        <tr>
          <td><h3>お名前</h3><p>予約時の情報として使用されます。</p></td>
          <td><%= f.label :name_family, '姓：' %><%= f.text_field :name_family, value: @user.name_family, readonly: true %></td>
          <td><%= f.label :name_first, '名：' %><%= f.text_field :name_first, value: @user.name_first, readonly: true %></td>
        </tr>
        <tr>
          <td></td>
          <td><%= f.label :name_family_kana, 'セイ：' %><%= f.text_field :name_family_kana, value: @user.name_family_kana, readonly: true %></td>
          <td><%= f.label :name_first_kana, 'メイ：' %><%= f.text_field :name_first_kana, value: @user.name_first_kana, readonly: true %></td>
        </tr>
        <tr>
          <td><h3><%= f.label :phone_number, '電話番号' %></h3></td>
          <td><%= f.text_field :phone_number, value: @user.phone_number, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td><h3><%= f.label :email, 'メールアドレス' %></h3><p>ログイン時のIDとして使用されます。また、予約の確認や、メールマガジンの配信も行われます。</p></td>
          <td><%= f.email_field :email, autocomplete: "email", value: @user.email, readonly: true %></td>
          <td></td>
        </tr>
        <tr>
          <td>
            <h3><%= f.label :password, 'パスワード' %></h3>
            <% if @minimum_password_length %>
              <em>(<%= @minimum_password_length %>文字以上)</em>
            <% end %>
          </td>
          <td><%= @password %></td>
          <td><%= f.hiddden_field :password, autocomplete: "new-password", value: @user.password %></td>
          <td></td>
        </tr>
      </tbody>
    </table>

    <div class="actions">
      <%= f.submit "修正する", class: 'btn btn-default', name: :back %>
      <%= f.submit "登録する", class: 'btn btn-primary' %>
    </div>
  <% end %>

  <%= render "public/users/shared/links" %>

</div>
